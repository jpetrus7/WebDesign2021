// Scratch for programmers
// Version 2.0


let cpu =  ["rock", "paper", "scissors"];

function randChoice(x){
    return Math.floor(Math.random()*x);
}

let cpuChoice = randChoice(cpu.length);

console.log(cpu[cpuChoice]);
console.log(cpu[playerChoice]);




